import 'package:flutter/material.dart';

class DonationsPage extends StatelessWidget {
  static const pageName = 'donations';
  const DonationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
