import 'package:flutter/material.dart';

class AddPage extends StatelessWidget {
  static const pageName = 'add_page';
  const AddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
